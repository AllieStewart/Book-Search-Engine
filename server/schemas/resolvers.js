// Start of JS file
// Resolvers for establishing Query and Mutation.
const { User, Book } = require('../models');

const resolvers = {
    Query: {

    },

    Mutation: {

    },
};

module.exports = resolvers;
// End of JS file